<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>ModelEditor | Admin Panel</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .admin-container { margin-top: 50px; max-width: 600px; }
        .admin-form { 
            background: var(--bg-card); 
            padding: 30px; 
            border-radius: 12px; 
            border: 1px solid var(--accent);
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        .admin-form input, .admin-form textarea {
            width: 100%;
            padding: 12px;
            background: #0b0e14;
            border: 1px solid #333;
            color: white;
            border-radius: 4px;
        }
        .admin-form h2 { margin: 0; color: var(--accent); }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="logo"><h1>Model<span>Admin</span></h1></div>
            <nav><a href="/" style="color:white">На главную</a></nav>
        </div>
    </header>

    <div class="container admin-container">
        <h1>Панель управления: <%= user.username %></h1>
        
        <form action="/add-post" method="POST" enctype="multipart/form-data" class="admin-form">
   <h2>Опубликовать новое обновление</h2>
            
            <label>Название обновления:</label>
            <input name="title" placeholder="Например: Big Physics Update" required>
            
            <label>Версия (для C++ апдейтера):</label>
            <input name="version" placeholder="Например: 0.4" required>
            
            <label>Описание изменений:</label>
            <textarea name="text" placeholder="Что нового в этой версии..." rows="5"></textarea>
            
            <label>Файл обновления (.zip):</label>
            <input type="file" name="updateZip" accept=".zip" required>
            
            <button type="submit" class="download-btn">Опубликовать для всех</button>
        </form>
    </div>
</body>
</html>